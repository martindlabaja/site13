[build]
base = "."
command = "node server.js"  # Adjust the build command as needed
publish = "."  # Assuming your built files are outputted to a "dist" directory
[[redirects]]
from = "/"
to = "public/index.html"
status = 200

